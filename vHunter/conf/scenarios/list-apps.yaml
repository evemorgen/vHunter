list-apps-scenario:
    job:
        ubuntu: dpkg -l | awk ''{ print $2 \",\" $3}'' | tail -n +6
        centos: yum list
        osx: brew list --versions | sed -e ''s/ /,/g''
        default: echo ''distro not found''

